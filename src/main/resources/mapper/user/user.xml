<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.heeroes.setset.user.model.mapper.UserMapper">

    <!-- 검색 -->
    <select id="findByEmail" parameterType="String" resultType="user">
        select id, nickname, email
        from user
        where email=#{email}
    </select>

    <select id="findById" parameterType="int" resultType="user">
        select id, nickname, email, oauth_provider, original_name, image_key
        from user
        where id=#{id}
    </select>

    <insert id="insert" parameterType="user">
        insert into user(nickname, email,oauth_provider, sns_connect_date)
        values(#{nickname}, #{email}, #{oAuthProvider}, now())
    </insert>
    
    <!--  refresh token 넣기 -->
	<update id="saveRefreshToken" parameterType="map"> 
	update user set refresh_token=#{refreshToken}
	where email=#{email} 
	</update>
    <update id="update" parameterType="user">
        update user
        set nickname=#{nickname},
            original_name=#{originalName},
            image_key=#{imageKey}
        where id = #{id}
    </update>
    
</mapper>
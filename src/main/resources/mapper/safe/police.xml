<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	
<mapper namespace="com.heeroes.setset.safe.model.mapper.PoliceMapper">
	<select id="searchSafeMapByKeyword" parameterType="String" resultType="police">
		select id, name, address, latitude, longitude, tel
		from police
		where address like concat('%', #{keyword}, '%')
		limit ${size}
	</select>
	
	<select id="searchSafeMapByLocation" parameterType="map" resultType="police">
		select id, name, address, latitude, longitude, tel
		from police
		where #{maxLat} >= latitude and latitude >= #{minLat}
		      and #{maxLong} >= longitude and longitude >= #{minLong}
		limit ${size}
	</select>

</mapper>